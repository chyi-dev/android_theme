{
  "name": "theme-demo-server",
  "version": "1.0.0",
  "private": true,
  "description": "Demo config service for dynamic Android theme colors + images",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
