<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Demo only: emulator (10.0.2.2) and LAN HTTP servers. Do not ship this to production. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
